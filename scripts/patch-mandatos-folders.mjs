import fs from 'node:fs'

const homePath = 'src/pages/Home.jsx'
const cssPath = 'src/styles/global.css'

let home = fs.readFileSync(homePath, 'utf8')
const startMarker = '<div className="previous-terms-grid">'
const endMarker = '{selectedMandato && ('

if (home.includes(startMarker) && home.includes(endMarker) && !home.includes('MANDATOS_FOLDER_UI')) {
  const start = home.indexOf(startMarker)
  const end = home.indexOf(endMarker, start)

  const folderBlock = `<div className="previous-terms-folders" data-ui="MANDATOS_FOLDER_UI">\n              {mandatosAgrupados.map(([mandato, itens]) => {\n                const metaMandato = itens.find(item => item._kind === 'mandato')\n                const registrosMandato = itens.filter(item => !item._kind || item._kind === 'registro')\n                const categoriasCriadas = itens.filter(item => item._kind === 'categoria').map(item => item.tipo).filter(Boolean)\n                const categoriasComRegistros = registrosMandato.map(item => item.tipo || 'Outros')\n                const categorias = [...new Set([...categoriasCriadas, ...categoriasComRegistros])].sort((a,b)=>a.localeCompare(b))\n\n                return (\n                  <details className="mandato-folder" key={mandato}>\n                    <summary className="mandato-folder-cover">\n                      <div>\n                        <span className="section-kicker">PASTA DO MANDATO</span>\n                        <strong>{mandato}</strong>\n                        <small>{registrosMandato.length} registro(s) arquivado(s)</small>\n                        {metaMandato?.descricaoMandato && <p className="mandato-folder-description">{metaMandato.descricaoMandato}</p>}\n                      </div>\n                      <span className="folder-open-label">Abrir pasta</span>\n                    </summary>\n                    <div className="mandato-subfolders">\n                      {categorias.map(tipo => {\n                        const registros = registrosMandato.filter(item => (item.tipo || 'Outros') === tipo)\n                        return (\n                          <details className="mandato-subfolder" key={tipo}>\n                            <summary><div><strong>{tipo}</strong><span>{registros.length} registro(s)</span></div><b>Abrir</b></summary>\n                            <div className="mandato-record-list">\n                              {registros.slice().sort((a,b)=>String(b.data||'').localeCompare(String(a.data||''))).map(item => (\n                                <button type="button" className="mandato-record" key={item.id} onClick={() => setSelectedMandato(item)}>\n                                  <div>\n                                    <span>{item.numero ? 'Nº ' + item.numero : 'Sem número'}</span>\n                                    <strong>{item.titulo || 'Registro do mandato'}</strong>\n                                    <small>{item.data || item.local || 'Data não informada'}</small>\n                                  </div>\n                                  <b>Ver detalhes</b>\n                                </button>\n                              ))}\n                              {!registros.length && <p className="mandato-empty-category">Nenhum registro publicado nesta subpasta ainda.</p>}\n                            </div>\n                          </details>\n                        )\n                      })}\n                      {!categorias.length && <p>Nenhuma subpasta criada neste mandato ainda.</p>}\n                    </div>\n                  </details>\n                )\n              })}\n              {!mandatosAgrupados.length && <p>Nenhum registro de mandato anterior publicado ainda.</p>}\n            </div>\n\n            `

  home = home.slice(0, start) + folderBlock + home.slice(end)
  fs.writeFileSync(homePath, home)
}

let css = fs.readFileSync(cssPath, 'utf8')
if (!css.includes('/* MANDATOS_FOLDER_STYLES */')) {
  css += `\n\n/* MANDATOS_FOLDER_STYLES */\n.previous-terms-folders{display:grid;grid-template-columns:repeat(auto-fit,minmax(280px,1fr));gap:22px}.mandato-folder{min-width:0}.mandato-folder-cover{list-style:none;cursor:pointer;min-height:210px;border-radius:26px;padding:28px;background:#fff;border:1px solid rgba(91,45,123,.12);box-shadow:0 14px 38px rgba(37,16,53,.08);display:flex;align-items:flex-end;justify-content:space-between;gap:18px}.mandato-folder-cover::-webkit-details-marker,.mandato-subfolder summary::-webkit-details-marker{display:none}.mandato-folder-cover strong{display:block;font-size:clamp(1.65rem,4vw,2.7rem);line-height:1.05;margin:.35rem 0 .55rem}.mandato-folder-description{max-width:580px;margin:8px 0 0;color:#6f6578;line-height:1.55;font-size:.9rem}.folder-open-label{font-weight:800;white-space:nowrap}.mandato-subfolders{display:grid;gap:13px;margin-top:14px}.mandato-subfolder{border:1px solid rgba(91,45,123,.12);border-radius:20px;background:#fff;overflow:hidden}.mandato-subfolder>summary{list-style:none;cursor:pointer;padding:18px;display:flex;align-items:center;justify-content:space-between;gap:12px;background:#faf7fc}.mandato-subfolder>summary div{display:grid;gap:3px}.mandato-record-list{display:grid;gap:9px;padding:12px}.mandato-record{width:100%;border:1px solid rgba(91,45,123,.1);background:#fff;border-radius:15px;padding:14px;text-align:left;display:flex;align-items:center;justify-content:space-between;gap:14px;cursor:pointer}.mandato-record div{display:grid;gap:4px;min-width:0}.mandato-record span{font-size:.72rem;font-weight:900;text-transform:uppercase}.mandato-record strong{font-size:.94rem;overflow-wrap:anywhere}.mandato-record>b{font-size:.78rem;white-space:nowrap}.mandato-empty-category{margin:4px;padding:10px;color:#776d7d;font-size:.88rem}@media(max-width:700px){.previous-terms-folders{grid-template-columns:1fr}.mandato-folder-cover{min-height:180px;padding:22px}.mandato-record{align-items:flex-start}}\n`
  fs.writeFileSync(cssPath, css)
}

console.log('[mandatos] pastas, descricoes, subpastas e registros aplicados.')
